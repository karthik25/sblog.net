@model List<sBlog.Net.Domain.Entities.TagEntity>

@if (Model.Count > 0)
{
    foreach (var tag in Model.Where(tag => tag.IsTagEntityValid()))
    {
        @:@Html.RouteLink(tag.TagName, "Tag", new { tagName = tag.TagSlug })&nbsp;
    }
}
else
{
    <b>--no tags--</b>
}