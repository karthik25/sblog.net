@model sBlog.Net.Models.BlogPostPageViewModel

@{
    ViewBag.Title = string.Format("{0} || page {1}", Model.BlogName.ToLower(), Model.CurrentPageNumber);
}

@Html.Partial("DisqusCounts", Model)

@if (!Model.Any)
{
    <div>@sBlog.Net.Resources.Views.Shared.GenericResources.NoPostsMessage</div>
}

@foreach (var post in Model.Posts)
{
    @Html.Partial("Post", post)
}
<br />

@if (Model.Any && Model.PreviousPageValid)
{
    <div style="float: left">
        @Html.RouteLink(sBlog.Net.Resources.Views.Shared.GenericResources.PreviousPageLink, "Page", new { pageNumber = Model.PreviousPageNumber }, new { @class = "link" })
    </div>
}

@if (Model.Any && Model.NextPageValid)
{
    <div style="float: right">
        @Html.RouteLink(sBlog.Net.Resources.Views.Shared.GenericResources.NextPageLink, "Page", new { pageNumber = Model.NextPageNumber }, new { @class = "link" })
    </div>
}
